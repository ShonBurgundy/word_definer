<h4>Word: <%= @word.name %></h4>

<h4>Definitions for this word:</h4>

  <% if @word.definitions %>
    <ul>
      <% @word.definitions.each do |definition| %>
        <li><a href="/words/<%= @word.id %>/definitions/<%= definition.id %>"><%= definition.name %></a></li>
      <% end %>
    </ul>
    <% else %>
      </p> There are no definitions for this word yet!</p>
    <% end %>

    <h4>Add a definition:</h4>

    <form action="/words/<%= @word.id %>/definitions" method="post">
      <div class="form-group">
        <input id="def" name="def" class="form-control" type="text" placeholder="Define Here">
      </div>
      <button type="submit" class="btn btn-info">Add Artist</button>
    </form>

    <p><a href="/words/<%= @word.id %>/edit">Edit Definition</a></p>
    <a href="/homepage">Return to homepage.</a>

